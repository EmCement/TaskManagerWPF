<Window x:Class="TaskManagerWPF.Views.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Профиль - Менеджер задач"
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Width="120"
                Height="120"
                Background="#3498db"
                CornerRadius="60"
                Margin="0,0,30,0">
            <TextBlock x:Name="AvatarText"
                      Text="AD"
                      FontSize="36"
                      Foreground="White"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Column="1">
            <TextBlock x:Name="UserNameText" 
                      Text="Загрузка..." 
                      FontSize="24" 
                      FontWeight="Bold"
                      Foreground="#2c3e50"
                      Margin="0,0,0,10"/>

            <Grid Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0"
                          Foreground="#7f8c8d"/>
                <TextBlock x:Name="LoginText" 
                          Text="Загрузка..." 
                          Grid.Row="0" Grid.Column="1"
                          FontWeight="SemiBold"/>

                <TextBlock Text="Email:" Grid.Row="1" Grid.Column="0"
                          Foreground="#7f8c8d"/>
                <TextBlock x:Name="EmailText" 
                          Text="Загрузка..." 
                          Grid.Row="1" Grid.Column="1"
                          FontWeight="SemiBold"/>

                <TextBlock Text="Роль:" Grid.Row="2" Grid.Column="0"
                          Foreground="#7f8c8d"/>
                <TextBlock x:Name="RoleText" 
                          Text="Загрузка..." 
                          Grid.Row="2" Grid.Column="1"
                          FontWeight="SemiBold"
                          Foreground="#e74c3c"/>

                <TextBlock Text="Дата регистрации:" Grid.Row="3" Grid.Column="0"
                          Foreground="#7f8c8d"/>
                <TextBlock x:Name="RegisterDateText" 
                          Text="Загрузка..." 
                          Grid.Row="3" Grid.Column="1"
                          FontWeight="SemiBold"/>
            </Grid>

            <StackPanel Orientation="Horizontal">
                <Button Content="Редактировать профиль"
                       Background="#3498db"
                       Foreground="White"
                       Width="180"
                       Height="35"
                       Margin="0,0,10,0"
                       Click="EditProfile_Click"/>

                <Button Content="Сменить пароль"
                       Background="#9b59b6"
                       Foreground="White"
                       Width="150"
                       Height="35"
                       Click="ChangePassword_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>